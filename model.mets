Process <- 1..3:
    begin read_committed
        minors := pi[count](sigma[age < 18](users))
        if minors <= 1
            xi(users){ age = 12 }
    commit

Property [](pi[count](sigma[age < 18](users)) <= 1)
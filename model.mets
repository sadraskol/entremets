Process <- 1..3:
    begin read_committed
    minors := count(sigma[age < 18](users))
    if minors <= 1
        xi(users){ age = 12 }
    commit

Property [](count(sigma[age < 18](users)) <= 2)


Process <- 1..3:
    begin read_committed
    minors := count(select * from users where age < 18)
    if minors <= 1
        insert into users(age) values (12)
    commit

Property always[count(select * from users where age < 18) <= 2]